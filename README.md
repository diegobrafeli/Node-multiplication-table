# Notes

```
Options:
      --help     Show Help           [booleano]
      --version  Show version        [booleano]
  -b, --base     It is the base the mutiply table [number] [required]
  -l, --list     Show the multiply table [booleano] [default: false]
  -u, --until    The number of time the base will be multiplied [number] [default: 10]
```